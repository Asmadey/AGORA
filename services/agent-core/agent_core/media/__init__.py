"""
Препроцессинг видео (задача #14).

Готовит загруженный ролик к разбору: снимает параметры, делает proxy для
покадрового анализа и извлекает звуковую дорожку для транскрипции.

─── Почему это отдельный модуль, а не часть оркестратора ──────────────────
Граф исполнения (#13) ВЫЗЫВАЕТ препроцессинг, а не наоборот. В графе задач
#14 висела за ребром на #13, из-за чего вся ветка медиа считалась
заблокированной цепочкой UI. Кодом эта зависимость не подтверждалась: здесь
нет ничего, кроме ffmpeg. Ребро снято 04.08.2026.

─── Общая шкала времени ───────────────────────────────────────────────────
Всё, что делает модуль, подчинено одному требованию из cdd: таймкод сцены,
найденной на proxy, обязан указывать на тот же момент в извлечённом аудио.
Отсюда два неочевидных решения:

· proxy пересобирается с ПОСТОЯННЫМ фреймрейтом. Исходник с камеры или из
  редактора часто имеет переменный (VFR): номер кадра там не переводится в
  секунды умножением, и таймкод «кадр 150» на 25 fps укажет не на 6.0 с, а
  куда попало. PySceneDetect считает именно по номерам кадров.

· start_time контейнера сбрасывается в ноль. mp4 из некоторых источников
  начинается с ненулевого времени представления, и тогда вся шкала сдвинута
  на эту величину — молча, без единой ошибки.

Ошибки — своим типом MediaError. Вызывающему нужно отличать «это не видео» от
«ffmpeg не установлен»: первое означает отказ пользователю, второе — поломку
развёртывания, и лечатся они по-разному.
"""

from __future__ import annotations

from .audio import extract_audio
from .errors import MediaError
from .probe import VideoInfo, probe
from .proxy import make_proxy, segment

__all__ = [
    "MediaError",
    "VideoInfo",
    "probe",
    "make_proxy",
    "extract_audio",
    "segment",
]
