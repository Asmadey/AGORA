-- AGORA · 10 · Засев промпта persona.enrich (обогащение персон моделью)
--
-- Отдельный файл, а не правка 07_prompts_seed.sql. Причина в §5 CLAUDE.md:
-- применённая миграция не редактируется. 07 к тому же генерируется скриптом
-- generate-prompts-seed.mjs, и перегенерация переписала бы его целиком — то
-- есть невидимо для ревью изменила бы тексты всех тринадцати промптов заодно.
--
-- Идемпотентно тем же способом, что 07: ON CONFLICT DO NOTHING поверх
-- partial-индекса prompts_default_key_uniq WHERE is_default. Повторный прогон
-- не плодит строк и не затирает версию, отредактированную командой в
-- Промпт-студии (#26).
--
-- Зачем промпт нужен: PersonaGenerator собирает заземлённый на корпус скелет
-- персоны, а модель переписывает единственное поле narrative. Границу держит
-- agent_core/persona/enrich.py — всё, по чему считается persona_grounding,
-- модель не видит и изменить не может.

INSERT INTO prompts (tenant_id, key, stage, template, variables, model_params, version, is_active, is_default)
VALUES (NULL, 'persona.enrich', 'Персоны', '# persona.enrich (narrative)
Переписывание поля narrative у персоны, СКЕЛЕТ которой уже собран из корпуса.
Переменные: {{skeleton_json}}, {{age}}, {{gender}}, {{city}}, {{geo}}, {{values}},
{{hobbies}}, {{work_status}}, {{min_len}}.
---
Ниже — синтетическая персона, собранная сэмплированием по реальным долям
исследовательского корпуса. Все её факты уже определены и менять их нельзя.

Твоя задача одна: написать связный портрет этого человека живым русским языком.

Факты персоны (единственный источник истины):
{{skeleton_json}}

Опорные признаки: {{age}} лет, {{gender}}, {{city}} ({{geo}}), {{work_status}}.
Ценности: {{values}}.
Увлечения: {{hobbies}}.

Требования к тексту:
- От третьего лица, одним абзацем, не короче {{min_len}} символов.
- Только факты из списка выше. Не добавляй профессию, семейное положение,
  доход, диагнозы и события биографии, которых там нет.
- Не перечисляй поля списком и не повторяй числовые баллы — покажи, как эти
  черты выглядят в поведении: что человек смотрит, как выбирает, чему верит.
- Без оценок «типичный представитель», «яркий пример» и прочих ярлыков
  исследователя. Это описание человека, а не строки в таблице.
- Никаких вводных вроде «Вот портрет:». Верни только сам текст портрета.
', '["skeleton_json","age","gender","city","geo","values","hobbies","work_status","min_len"]'::jsonb, '{}'::jsonb, 1, true, true)
ON CONFLICT DO NOTHING;
