# START HERE — AGORA

Точка входа для нового диалога / агента-исполнителя. Прочитай этот файл первым.
**Ревизия: 27.07.2026** — граф вырос до 31 задачи, добавлены 5 фич, устранены расхождения
между документами (журнал — `docs/AGORA_HANDOFF.md` §12).

## Стартовый промпт (вставь в новый чат)
> Прочитай `docs/AGORA_HANDOFF.md` и `docs/AGORA_PRD_FINAL.md`, затем операционные артефакты:
> `docs/AGORA_loop_goal.md`, `evals/check.py`, `prompts/`.
> Это проект AGORA — self-host сервис синтетических фокус-групп для оценки видео. Все ключевые
> решения зафиксированы в Decision Log (§3 FINAL / §2 HANDOFF), фичи ревизии — в §20 FINAL /
> §11 HANDOFF. Восстанови граф задач из `evals/state/tasks.json` (31 задача,
> поле `blockedBy`). Реализуем через `/goal`-петлю (вариант B — петля запускается в среде
> пользователя, не в песочнице). Не начинай кодить, пока не подтвердишь, что понял контекст,
> и не задашь уточняющие вопросы.

## Карта файлов
**Читать по порядку:**
1. `docs/AGORA_HANDOFF.md` — решения, контракт сборки, карта нумерации задач, что реально
   готово, журнал расхождений, что нужно от пользователя.
2. `docs/AGORA_PRD_FINAL.md` — единый PRD (продукт + технический + правки интервью + §20 фичи).
3. `docs/AGORA_loop_goal.md` — `/goal`-петля (верификатор, состояние, остановка, режимы).
4. `evals/check.py` — внешний верификатор (`python evals/check.py`).
5. `evals/state/tasks.json` — **граф из 31 задачи** с зависимостями, фазами,
   привязкой к метрикам (`verifies`) и CDD-тестами. Источник истины по прогрессу.
6. `prompts/` — **13 seed-промптов** (реестр Промпт-студии).
7. `data/grounding/unified_respondent_sessions.json` — grounding-корпус (165).

**История решений (контекст, необязательно):** `docs/AGORA_PRD_v1.3.md`, `v1.4.md`,
`docs/AGORA_Анализ_репозиториев_и_ТЗ_v0.2.md`.

## TL;DR решений
Qwen 3.6 via timeweb API (VLM+персоны) + Whisper self-host CPU · оба режима видео,
e2e_short первым · Valkey+Celery+SSE · монорепо apps/web+services/agent-core · PySceneDetect+
панели×4+кэш · faster-whisper large-v3 · 8-категорийная Persona DNA (Big Five 1–5) ·
групповой синтез (не AI-модератор) · Auth.js «команда=арендатор»+RLS · «Перекрытие»
(replication) + golden-сеты×3 · статическая калибровка · петлю гоняет исполнитель (вариант B).

## Фичи ревизии 27.07.2026 (задачи #28–#31, все ПОСЛЕ гейта e2e_short)
- **#28 Чат по результатам** — два режима: аналитик по всему исследованию и допрос
  конкретной персоны из её карточки. Отдельный агент, не оркестратор.
- **#29 «Поделиться»** — публичная read-only ссылка: токен + TTL + revoke + аудит.
  Единственный санкционированный обход RLS.
- **#30 Перезапуск с новыми вопросами** — та же аудитория и тот же разбор видео (VLM не
  пересчитывается); память персон на выбор: «допрос» или «чистый прогон».
- **#31 Файл контекста для аудитории** — дистилляция в портрет → в `persona.generate`.
  Корпус приоритетнее файла: файл уточняет, но не переопределяет заземление.
- Плюс расширение **#6**: карточка персоны показывает ВСЕ атрибуты, а не подмножество.

## Текущий статус (проверено запуском)
`python evals/check.py` → `pass=2 fail=1 skip=12`, green=False (Pass 1 завершён).
`secret_scan` ✅ · `build_worker` ✅ · `tasks_done` ❌ (0/31) · остальные 12 — skip.
Задача #1 (монорепо + compose) реализована, ждёт подтверждения в вашей среде:
`compose_health` и `build_frontend` требуют docker и npm ci.

## Что нужно от пользователя перед стартом
- ✅ Доступ к Qwen через timeweb (OpenAI-совм. baseURL) — подтверждён.
- ✅ `unified_respondent_sessions.json` в `data/grounding/` — на месте.
- ⬜ Managed БД (Postgres 18 / Mongo 8 / Valkey 9.1) + S3-бакет на TimeWeb.
- ⬜ Новый GitHub-репозиторий (чистая история) для монорепо.
- ⬜ Реальный ролик ~12 мин → `evals/fixtures/long_12min.mp4` (сейчас его нет).
- ⚠️ **Перевыпустить два утёкших ключа** (`AIza…` и `sk-…`): они лежали в `backup/`
  со старыми репозиториями. Каталог удалён, но удаление не отменяет утечку.
  Рабочий ключ переехал в `.env.local` (закрыт `.gitignore`).
