# /goal — ночной прогон 02→03.08.2026

Редакция `/goal` для автономной работы, пока человек недоступен. Отличается от
`docs/AGORA_loop_goal.md` тремя вещами: блоком фактического состояния, жёсткой рамкой
ночной автономии и приоритетом на этот прогон. Всё остальное — оттуда, без изменений.

---

```
/goal

── СОСТОЯНИЕ НА НАЧАЛО ПРОХОДА (не проверять заново, это факты) ──
  main = c373f80. PR #4 и #5 слиты. В графе 13 задач из 31 помечены done.
  Миграции 07_prompts_seed и 08_portrait_versions применены на VPS через migrate.sh,
    13 дефолтных промптов засеяны, таблица audience_portrait_versions создана.
  Дамп базы снят перед миграциями.
  17 таблиц из 18 с FORCE RLS; users без RLS — задокументированное исключение
    (глобальная сущность, изоляция обеспечивается через team_members). Это НЕ дефект.
  ОТКРЫТЫЙ БЛОКЕР: docker-сборка web падает — «Can't resolve 'mongodb'».

  Диагноз блокера (проверен, повторно не расследовать):
    mongodb@7.5.0 установлен ТОЛЬКО как apps/web/node_modules/mongodb — npm его не
    поднял в корень. infra/web.Dockerfile на строке 13 копирует из deps-стадии
    исключительно /repo/node_modules, а apps/web/node_modules не копирует: эту строку
    убрали коммитом 310f805 в предположении, что npm ci --workspaces складывает всё в
    корень. Для mongodb предположение неверно.
    Лечится восстановлением копирования вложенных зависимостей воркспейса:
      COPY --from=deps /repo/apps/web/node_modules ./apps/web/node_modules
    Точечный COPY на конкретный пакет не годится: в runner-стадии уже есть такой костыль
    для hash-wasm (строка 29) — это второй случай той же болезни, третий будет.
    Дело НЕ в lockfile, НЕ в доступе к MongoDB и НЕ в пароле от неё.

── ПРИОРИТЕТ ЭТОГО ПРОХОДА ──
  Новые задачи графа НЕ БРАТЬ. Долг подтверждения важнее нового кода: 13 задач стоят
  done, подтверждённые только статикой, и мы уже трижды видели, что за этим прячется —
  маршрут, который не компилируется; сборщик датасета, дающий 168 записей вместо 165;
  миграция, которая не применяется. Порядок работ:

  1. Починить docker-сборку по диагнозу выше. Отдельная ветка, PR, зелёный CI.
  2. Поднять стек на VPS, дождаться healthchecks.
  3. Прогнать ПОВЕДЕНЧЕСКИЙ уровень CDD-тестов задач #4, #5, #7, #8, #10, #23, #24,
     #25, #26, #27 с реальными DATABASE_URL, MONGODB_URL, BASE_URL и посеянными
     пользователями (owner + member).
  4. Выставить status в evals/state/tasks.json ПО ФАКТУ: прошло поведенчески — done,
     не прошло или не запускалось — pending. Одним PR, с перечнем доказательств.
     Сейчас #24 стоит done при #23 в pending — это противоречие закрыть тем же PR.
  5. python3 evals/check.py — зафиксировать schema_drift, rls_tenant, prompts_editable.
  6. Записать проход в loop-state.json одним коммитом.

── НОЧНАЯ РАМКА (человек недоступен, подтвердить решение некому) ──
  РАЗРЕШЕНО без спроса: писать код и тесты, заводить ветки, открывать PR, применять
    миграции через migrate.sh, пересобирать и перезапускать контейнеры, снимать дампы,
    гонять поведенческие тесты, править документы.
  ЗАПРЕЩЕНО до утра, при необходимости — стоп-доклад и ожидание:
    · force-push и удаление любой удалённой ветки, включая свою;
    · reset --hard, checkout --, clean -fd, stash drop при непустом рабочем дереве;
    · слияние PR при красном CI — независимо от того, насколько убедительно объяснение,
      почему CI неправ;
    · слияние собственного PR без зелёного CI;
    · DDL руками (psql, панель провайдера) в обход нумерованной миграции;
    · удаление или перезапись данных в живой базе, ротация ключей;
    · массовые платные вызовы моделей (гейт CANARY остаётся: ≤ 3 реальных вызова).

  ── Красный CI блокирует слияние. Без исключений ──
  Сегодня PR #4 был слит при красном задании «CDD-тесты задач» с формулировкой
  «это не дефект кода PR, а конфигурация секретов». Так делать нельзя по двум причинам.
  Во-первых, это ровно то «я считаю, что готово», которое /goal запрещает: вердикт
  выносит верификатор, а не рассуждение о верификаторе. Во-вторых, объяснение было
  неверным по существу: задание cdd не запускает docker compose вообще, compose
  вызывается только внутри check.py, а check.py в CI гоняет задание secrets — то есть
  диагноз относился к другому заданию.
  Правильный порядок: если CI неправ — отдельный маленький PR, чинящий CI, слить его,
  дождаться зелёного, и только потом сливать исходный. Если причина непонятна — стоп-доклад
  с логом упавшего шага.

── ОСТАНОВКА ──
  Стоп-доклад (не жечь остаток лимита) при любом из:
    · одна и та же ошибка два прохода подряд либо две неудачные попытки одного фикса;
    · требуется действие из запрещённого списка;
    · поведенческий прогон показал расхождение с тем, что записано в графе, и непонятно,
      чему верить;
    · нужны креды, которых нет в .env.local.
  Доклад: что сделано, метрики check.py, что осталось неподтверждённым, гипотеза блокера.

── ЧТО ЗНАЧИТ «ЗАДАЧА DONE» ──
  Два уровня CDD: статический (работает без БД) и поведенческий (живые БД, сервер, S3,
  модель). Отсутствие среды обязано давать SKIP, а не выдуманный результат.
  SKIP — это не pass. Статический green необходим, но не достаточен.
  done ставится только после зелёного поведенческого уровня в живой среде.
  Автор не закрывает свою задачу сам.

── ГРАНИЦЫ ──
  Не выдумывай: только реальные ответы API и моделей либо зафиксированные фикстуры.
  Схема БД меняется только нумерованной миграцией в infra/postgres/init/, применяемой
    через migrate.sh. Расхождение живой схемы с репозиторием ловит метрика schema_drift.
  git add -A — только после git status: в рабочий каталог попадают посторонние файлы,
    и они уезжают в историю под чужим сообщением коммита (так уже уехали три XML).
  Нативные npm-модули в apps/web запрещены: каталог node_modules общий между macOS и
    Linux, установка из одной среды ломает другую.
  Отчётность: после прохода — запись в loop-state.json и PR с разделом «что НЕ
    подтверждено». Молчание не годится: над репозиторием работает несколько агентов,
    и незаписанное для остальных не существует.

── ОТКАЗНЫЕ ВЕТКИ ──
  0 строк из заведомо непустой таблицы → это RLS, а не потеря данных: смотри роль запроса
    и наличие политики в pg_policies. Лечится политикой для роли, НЕ снятием FORCE и
    НЕ выдачей BYPASSRLS.
  permission denied на ALTER ROLE … NOSUPERUSER/NOBYPASSRLS → на managed суперпользователя
    нет; проверяй pg_roles, не принуждай.
  docker compose config «required variable … is missing a value» → нужен --env-file .env.local.
  Тест падает трассировкой из-за старой библиотеки → дефект теста: обязан быть SKIP.
  Модуль не резолвится при docker-сборке, но есть локально → смотри, из какой стадии и
    какие каталоги node_modules копирует Dockerfile; вложенные зависимости воркспейса
    в корневой node_modules не попадают.
  429 / limit → backoff + retry до 3, затем перенос прохода. 403 → перечитать ключ из env,
    не вышло → стоп-доклад.

── УТРОМ ──
  Первым делом доложить: что из пунктов 1–6 закрыто, какие задачи перешли в done и по
  каким доказательствам, какие вернулись в pending, что осталось неподтверждённым.
```
