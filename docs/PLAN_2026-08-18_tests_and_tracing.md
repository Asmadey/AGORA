# План: остаток задач, тесты, трассировка и LangFuse

Составлен 18.08.2026. Статус проекта — в `docs/PROGRESS_REPORT.md`, проходы — в
`loop-state.json`. Этот документ не повторяет статус, он планирует работу.

---

# 0. Срочное: parakeet убивает воркер на длинном ролике

**Найдено ядром, а не догадкой.** `dmesg` на боевом сервере:

```
[Tue Aug 18 11:42:01 2026] Out of memory: Killed process 793832 (python3) anon-rss:8668124kB
[Tue Aug 18 12:03:40 2026] Out of memory: Killed process 810406 (python3) anon-rss:8728392kB
[Mon Aug 17 16:46:38 2026] Out of memory: Killed process 207022 (celery)  anon-rss:6714856kB
```

Первые две записи — мои прогоны parakeet на `15 min.mp4` (916 секунд звука). Обе
съели **по 8,7 ГБ** и были убиты на машине с 11 ГБ. Третья, вчерашняя, — сам
воркер под celery на 6,7 ГБ.

**Что это значит.** `sherpa_onnx.OfflineRecognizer` декодирует дорожку ЦЕЛИКОМ и
держит в памяти выход энкодера на всю её длину. На трёхминутном ролике это 24,7
секунды и несколько сотен мегабайт — отсюда мой замер и вывод «в восемь раз
быстрее». На пятнадцатиминутном — восемь с лишним гигабайт и смерть процесса.

**И я сделал parakeet моделью по умолчанию.** То есть прямо сейчас длинный ролик
уронит воркер по памяти, а выглядеть это будет случайным сбоем: Celery
перезапустит задачу, она умрёт снова, и в логе не будет ничего, кроме обрыва.

**Что делаю первым делом, до всего остального:**

1. **Порезать дорожку на куски.** Кусок по 120 секунд с перекрытием 2 секунды,
   таймкоды сдвигаются на смещение куска. Память перестаёт зависеть от длины
   ролика, а скорость сохраняется — модель одна и та же.
2. **Красный тест на память и на склейку:** пятнадцатиминутная дорожка обязана
   распознаться, а таймкоды последнего куска — совпасть с длительностью. Без
   первого условия дефект вернётся молча, без второго склейка кусков сдвинет
   весь транскрипт.
3. **Замерить заново на 15 минутах** — и только после этого утверждать что-либо
   про скорость на длинных материалах. Прежний замер верен для трёх минут и не
   переносится.

**Оговорка, которую надо сказать прямо.** Мой вывод «parakeet в восемь раз
быстрее» был получен на трёхминутной дорожке и подан как свойство модели. Это
свойство модели НА КОРОТКОМ материале. Проверять надо было на самом длинном, а не
на самом удобном.

---

# 1. Остаток продуктовых задач

## 1.1. Из последнего списка владельца

| № | Что | Состояние |
|---|---|---|
| 6 | Тихая речь в начале ролика теряется | Причина найдена: Silero VAD слышит первую речь на 112,1 с. Правка зависит от 0 — parakeet без VAD может закрыть пункт сам |
| 8 | Номер вместо UUID в адресе `/runs/0050` | Не начато. Имя файла в списке — сделано |
| 16 | Конструктор связей | Жду выбор из трёх вариантов (см. §5) |

## 1.2. Долги прошлых редакций

* **Golden-сет 3 из 3** — приёмка, закрывается прогоном в вашей среде.
* **Отчёт показывает выживших и не говорит, что показывает остаток.** Владелец
  увидел три персоны из двенадцати и решил, что прогон ненастоящий. Число
  исключённых есть в `qa_summary`, но на экране оно не рядом с составом
  аудитории.
* `SETTINGS_SECRET` в `.env.local` — без него ключ провайдера не сохранить.
* `docs/updates/last.md` — 528 строк с совпадениями по образцам пароля, лежит в
  дереве вне `.gitignore`.

---

# 2. Что покрыть тестами

## 2.1. Как выбирались места

Не «поднять покрытие», а закрыть те места, где ошибка **не видна на глаз**.
Критерий: дефект проходит мимо человека, потому что результат выглядит
правдоподобно. Все находки этой сессии — ровно такие, и все они были в местах из
списка ниже.

## 2.2. Воркер: 49 модулей, тестов нет у 14

Покрыты: конфигурация, транскрипция, сцены, кадры, пакет, персоны, QA, аналитика,
секреты, хранилище, воспроизводимость.

**Без тестов и с высокой ценой ошибки:**

| Модуль | Чем опасен | Что проверять |
|---|---|---|
| `asr/diarize.py` | 1111 секунд из 2024 в прогоне № 0050 — самый дорогой узел, и ни одной проверки | Дорожка с двумя голосами: спикеров ровно два, границы не выходят за длительность, спикер не меняется внутри реплики |
| `matching/finder.py` | Подбор персон под запрос — результат правдоподобен всегда | На известном корпусе: заданный срез возвращает персон этого среза, пустой запрос не возвращает всех |
| `grounding/corpus.py` | Из него считаются доли, по которым сэмплируются персоны | Битая запись не роняет разбор; доли суммируются в единицу; неизвестная категория не размывает остальные |
| `analytics/store.py` | Пишет отчёт в Mongo — отказ виден как «отчёт не открывается» | Запись и чтение того же документа; отказ Mongo не роняет прогон |
| `pipeline/graph.py` | Порядок узлов; ошибка = пропущенный этап | Каждый узел графа достижим; ни один не остался без входа |
| `pipeline/checkpoint.py` | Возобновление после перезапуска | Прогон, прерванный на середине, продолжается с того же узла |
| `pipeline/progress.py` | Экран прогресса и SSE | События идут по порядку; после REPORT_READY новых нет |
| `media/audio.py` | Дорожка для всей транскрипции | 16 кГц моно на входе любого формата; файл без звука — внятный отказ |
| `frames/dedup.py` | Отсев одинаковых кадров — режет стоимость разбора | Два одинаковых кадра схлопываются, два разных нет |
| `persona/generator.py` | Сэмплирование DNA из долей | Один seed даёт один набор; доли набора близки к долям корпуса |
| `portraits/distill.py` | Портреты сегментов | Без ключа провайдера возвращает None, а не падает |
| `schemas/*.py` | Контракты ответов модели | Ответ вне схемы отвергается с указанием поля |
| `survey.py` | Разбор анкеты — источник трёх дефектов этой сессии | Все три формы ключа; вопрос без label; пустая анкета |
| `secrets.py` | Уже покрыт, но не на стыке с воркером | Ключ, зашифрованный вебом, читается воркером в реальном прогоне |

**Порядок:** диаризация → survey → generator → checkpoint → остальное. Первые две
дают больше всего: одна самая дорогая, вторая уже трижды ломалась.

## 2.3. API: 34 маршрута, поведенческих тестов почти нет

Статические проверки есть (маршрут существует, зовёт `requireSession`). Живых
вызовов через настоящий HTTP — нет, кроме `evals/e2e_run.py`.

**Обязательные к покрытию, по цене ошибки:**

| Маршрут | Почему |
|---|---|
| `POST /api/tasks` | Создание прогона: идемпотентность, снимки промптов и настроек, номер по арендатору |
| `POST /api/audience` | Генерация персон: фоновая задача, прогресс, отказ на пустом датасете |
| `DELETE /api/audience`, `DELETE /api/personas` | Удаление: чужое не удаляется, занятый набор отклоняется |
| `PUT /api/settings` | Ключ провайдера: наружу не отдаётся, пустое поле не стирает |
| `POST /api/upload/presign` + `complete` | Подпись и ffprobe: файл без видео отвергается |
| `GET /api/tasks/[id]/progress` | SSE: снимок первым событием, переподключение не теряет состояние |
| `GET /api/tasks/[id]/report`, `/timeline`, `/transcript` | Чужой прогон неотличим от несуществующего |
| `POST /api/prompts/[key]/activate` | Активная версия одна; активация чужого промпта отклоняется |
| `GET /api/models` | Ключ не выходит наружу ни в каком виде |
| `/api/users`, `/api/users/[id]` | Роли: участник не заводит пользователей, последний владелец не удаляется |

**Как проверять.** Три уровня, и они не заменяют друг друга:

1. **Контрактный** — тело запроса и ответа против OpenAPI. Ловит расхождение
   спецификации с кодом, из-за которого клиенты строятся по вымыслу.
2. **Ролевой** — тот же вызов от владельца и от участника. Ловит дыры в правах,
   которые не видны, пока не зайдёшь участником.
3. **Изоляционный** — тот же вызов от двух арендаторов. Ловит утечку между
   командами: RLS работает, пока запрос идёт от нужной роли, и это ровно то, что
   ломается при рефакторинге.

Третий уровень важнее первых двух. Утечка между арендаторами — единственный
дефект в этом списке, который нельзя починить извинением.

---

# 3. Трассировка: что и зачем

## 3.1. Чего сейчас нет

Есть тайминги узлов (`progress.timings`) и `degraded`. По ним видно, что этап шёл
1111 секунд, и не видно **из чего** эти секунды состоят: сколько вызовов модели,
сколько токенов, сколько стоила пересборка забракованных ответов, какой промпт
уехал в конкретный вызов.

Каждый разбор этой сессии упирался в одно и то же: чтобы понять причину, я лез в
`qa_report.json` внутри контейнера, который умирает вместе с прогоном. Трассировка
— это тот же разбор, но без раскопок.

## 3.2. Что покрывать

**Каждый вызов модели** — минимальная единица. Их семь видов, и у каждого своя
цена ошибки:

| Вызов | Что писать в трассу |
|---|---|
| `frames.analyze` | Номер панели, таймкоды, модель зрения, токены, `parse_failed` |
| `persona.enrich` | Идентификатор персоны, seed, температура |
| `persona.validate` | Вердикт, confidence, номер попытки |
| `respondent` | Персона, повтор, температура, `finish_reason`, обрыв по потолку |
| `qa.*` | Вид проверки, вердикт, источник (правило или судья), эскалация |
| `analytics.report` | Сколько ответов дошло, сколько исключено |
| `portrait.distill` | Сегмент, размер группы |

**Обязательные атрибуты у всех:** `task_id`, `tenant_id`, имя и версия промпта из
снимка, модель, температура, токены на вход и на выход, длительность.

**Чего в трассах быть не должно:** ключа провайдера, текста персональных данных
из датасета, содержимого `.env`. Трасса живёт дольше прогона и смотрится шире —
секрет, попавший туда, не отзывается.

## 3.3. Иерархия

```
прогон (task_id)
├── probe_and_normalize
├── transcribe_and_diarize
│   ├── transcribe (модель, длительность, число сегментов)
│   └── diarize    (число спикеров)
├── analyze_chunks
│   └── vlm-вызов ×N (панель, токены)
├── evaluate_personas
│   └── respondent ×N (персона, повтор, токены, finish_reason)
├── qa
│   ├── правила (вердикты без вызова модели)
│   └── судья ×N
└── analytics
```

По такому дереву видно то, чего сегодня не видно ни из чего: **где кончаются
деньги** и **почему доля выживших такая**.

---

# 4. LangFuse на сервере

## 4.1. Ограничение, которое надо назвать до начала

Владелец просит `http://agora.185-154-194-125.sslip.io/langfuse`. **LangFuse не
поддерживает работу из подкаталога.** Это Next.js-приложение без настройки
`basePath`: его собственные ссылки, статика и OAuth-редиректы строятся от корня,
и проксирование `/langfuse` даст экран, который грузится наполовину. Обходить это
переписыванием HTML на nginx — способ получить неотлаживаемый стенд.

**Три честных варианта:**

| Вариант | Адрес | Цена |
|---|---|---|
| Поддомен (рекомендую) | `https://langfuse.185-154-194-125.sslip.io` | Отдельный блок nginx и сертификат Certbot — двадцать минут |
| Отдельный порт | `https://agora.185-154-194-125.sslip.io:3001` | Без нового сертификата, но порт открывается наружу |
| Подкаталог | как просили | Требует форка LangFuse с `basePath`; ломается на каждом их обновлении |

Прошу выбрать. По умолчанию делаю поддомен.

## 4.2. Что нужно LangFuse

Собственная база PostgreSQL и ClickHouse (с версии 3), Redis и S3-совместимое
хранилище. У нас всё это есть, но **мешать его с рабочим нельзя**: LangFuse под
нагрузкой пишет много, и общая база означала бы, что тормоза трассировки
выглядят тормозами продукта.

Отдельный `docker-compose.langfuse.yml`: свой Postgres, свой ClickHouse, свой
MinIO, своя сеть. Валки можно переиспользовать — он у нас почти пустой.

**Память.** LangFuse со всем набором — примерно 2,5–3 ГБ. Сейчас на машине 11 ГБ,
из них воркер под нагрузкой берёт до 7 (а с parakeet на длинном ролике брал 8,7 и
был убит). **Ставить LangFuse до починки §0 нельзя** — он отнимет ровно ту память,
которой воркеру не хватило.

Это не отговорка, а порядок: сначала §0, потом LangFuse.

## 4.3. Как подключается воркер

`langfuse` — Python-SDK, декоратор `@observe` поверх функций и `langfuse.openai`
вместо прямого клиента OpenAI. Второе даёт токены и стоимость без единой строки
учёта.

Три требования к подключению:

1. **Отказ трассировки не роняет прогон.** Недоступный LangFuse — это потеря
   наблюдаемости, а не потеря исследования. SDK умеет очередь с отбрасыванием;
   это надо включить явно и проверить тестом с заведомо мёртвым адресом.
2. **Ключи — из окружения**, `LANGFUSE_PUBLIC_KEY` / `LANGFUSE_SECRET_KEY` /
   `LANGFUSE_HOST`, как всё остальное (§7 CLAUDE.md).
3. **Выключается одной переменной.** `LANGFUSE_ENABLED=false` должен полностью
   убирать накладные расходы, иначе отладка проблем производительности начнётся с
   вопроса «а это не трассировка ли».

## 4.4. Порядок установки

1. `infra/docker-compose.langfuse.yml` — свой стек, отдельная сеть, тома с
   именами `langfuse_*`.
2. Блок nginx на поддомен + `certbot --nginx`.
3. Первый вход, создание проекта, выпуск ключей. **Ключи в переписку не идут** —
   владелец кладёт их в `.env.local` сам (§6-бис).
4. Подключение воркера: `langfuse.openai` в `ModelConfig`, `@observe` на узлах
   графа.
5. Проверка: один короткий прогон и дерево в интерфейсе, где видно все семь видов
   вызовов и суммарные токены.

---

# 5. Что нужно от владельца

**Конструктор связей (п. 16).** Три варианта, они не пересекаются по объёму:

1. **Связи внутри отчёта** — линия от числа к ответам, из которых оно посчитано, и
   дальше к таймкоду. День работы.
2. **Связи между сущностями** — датасет → набор → исследование → отчёт как граф.
   Два-три дня.
3. **Редактор схемы** — какое поле датасета во что превращается при сборке
   персоны. Меняет модель данных, отдельный проход.

**Адрес LangFuse** — поддомен, порт или форк (§4.1).

---

# Порядок работ

1. **§0** — чанкинг parakeet. Пока не сделано, длинные ролики падают.
2. **§1.1 п. 6** — тихое начало ролика, замер после §0.
3. **§1.1 п. 8** — номер в адресе.
4. **§2.2** — тесты воркера: диаризация, survey, generator, checkpoint.
5. **§2.3** — тесты API, начиная с изоляции арендаторов.
6. **§4** — LangFuse после §0.
7. **§3** — трассировка поверх поднятого LangFuse.
