import { EmptyState } from "@/components/agora/States";
import { FileQuestion } from "lucide-react";

/**
 * Страница 404.
 *
 * Формулировка «не существует или принадлежит другой команде» выбрана не из
 * вежливости. Обработчики отдают чужому арендатору именно 404, а не 403: ответ
 * «доступ запрещён» подтверждал бы, что объект с таким идентификатором есть, и
 * перебором можно было бы узнать состав чужих прогонов. Текст на экране обязан
 * говорить то же, что и код ответа, иначе один из двух врёт.
 */
export default function NotFound() {
  return (
    <div className="p-8">
      <EmptyState
        icon={<FileQuestion className="h-5 w-5" />}
        title="Страница не найдена"
        description="Такого адреса нет. Если вы перешли по ссылке на прогон, персону или проект — объект мог быть удалён или принадлежит другой команде."
        action={{ href: "/", label: "К списку прогонов" }}
      />
    </div>
  );
}
