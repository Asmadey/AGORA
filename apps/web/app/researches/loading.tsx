import { SkeletonHeader, SkeletonList } from "@/components/agora/States";

/**
 * Загрузка списка исследований.
 *
 * Форма повторяет будущий список: шапка и карточки той же высоты, что строки
 * прогонов. Когда данные приезжают, страница не прыгает — место уже занято.
 *
 * Файл заведён здесь, а не унаследован от корня, хотя раньше именно корневой
 * скелетон обслуживал этот экран: список прогонов жил на `/`, а когда уехал
 * на собственный адрес, скелетон за ним не поехал. Корневой при этом остался
 * запасным для ВСЕХ разделов, сохранив форму одного — то есть на «Настройках»
 * и «Датасете» рисовал четыре карточки прогонов.
 */
export default function Loading() {
  return (
    <>
      <SkeletonHeader />
      <div className="p-8">
        <SkeletonList rows={4} height="h-28" />
      </div>
    </>
  );
}
