<!--
Этот PR читает не только человек, но и другой ИИ-агент, который продолжит работу
с того места, где вы остановились. Для него важнее всего два раздела: что осталось
неподтверждённым и что выяснилось по дороге. «Всё сделано, тесты зелёные» не
сообщает ничего.
-->

## Задача

Номер из `evals/state/tasks.json`: #

Что сказано в `acceptance`:

## Что сделано

<!-- По пунктам, с указанием файлов. Не пересказ диффа, а решения: что выбрано и почему. -->

## Проверено

- [ ] CDD-тест `evals/tests/test_taskNN_*.py` — статический уровень зелёный
- [ ] `npx tsc --noEmit` (если менялась веб-часть)
- [ ] `npm run build`
- [ ] `pytest -q` и `ruff check .` в `services/agent-core` (если менялся воркер)

Вывод CDD-теста:

```
```

## Что НЕ подтверждено

<!--
Обязательный раздел. Поведенческие проверки, требующие живой базы, поднятого
сервера или настоящей модели, выполняются в среде пользователя. Перечислите, что
именно осталось непроверенным и какой командой это проверяется.

Задача не помечается `done` в tasks.json, пока этот раздел непустой.
-->

## Findings

<!--
Что выяснилось по дороге, включая опровергнутые гипотезы. Отрицательный результат
экономит следующему агенту целый проход; «сработало» не экономит ничего.
Эти же пункты идут в loop-state.json.
-->

## Миграции

- [ ] Схема БД не менялась
- [ ] Менялась — добавлен новый нумерованный файл в `infra/postgres/init/`,
      идемпотентный, применяется через `migrate.sh`; вручную в базе ничего не правилось
